# krtica server (the molehill) — runs on the machine with a public IP.
listen: "127.0.0.1:7000" # agents dial here (use :7000 on a real VPS)
token: "change-me"
tunnels:
  - name: web # must match a service name an agent advertises
    listen: "127.0.0.1:8080" # public visitors connect here
